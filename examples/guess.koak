def ask(): int
  print("Guess the number: ") :
  parse(readline());

n = random() % 10;
guess = ask();

while guess != n do
  (if guess < n then print("Too low!") else print("Too high!")) :
  guess = ask();

print("You got it!")
